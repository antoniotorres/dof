# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/
*.tsbuildinfo

# production
/build

# misc
.DS_Store

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
.env.local
.env.development.local
.env.test.local
.env.production.local
.env
.gstack/

# deployment / platform
.vercel

# editors
.idea/
*.swp

# secrets / certificates (never commit these)
*.pem
*.key
.env*
# Exception: a PUBLIC CA intermediate (no private-key material). Required by
# `pnpm ingest` to complete dof.gob.mx's incomplete TLS chain. Safe to commit.
!scripts/dof-godaddy-g2-intermediate.pem
